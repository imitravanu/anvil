{
  "name": "@anvil/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "anvil": "bin/anvil.cjs"
  },
  "files": [
    "dist",
    "bin"
  ],
  "scripts": {
    "build": "tsc -p . --noEmit && esbuild src/index.tsx --bundle --platform=node --format=esm --outfile=dist/index.js --alias:react-devtools-core=/home/mitravanu/Projects/anvil/packages/cli/src/devtools-shim.js --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\"",
    "dev": "tsx src/index.tsx",
    "prepublishOnly": "npm run build",
    "typecheck": "tsc -p . --noEmit"
  },
  "devDependencies": {
    "@anvil/core": "0.1.0",
    "@anvil/tui": "0.1.0",
    "ink": "^5.1.0",
    "react": "^18.3.1",
    "esbuild": "^0.28.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
