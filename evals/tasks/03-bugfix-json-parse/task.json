{
  "name": "Add safe fallback on JSON parse failure",
  "category": "bugfix",
  "prompt": "In src/parser.js, safeParseJson(str, fallback) crashes if str is invalid JSON. Wrap JSON.parse in try/catch so it returns fallback if parsing fails.",
  "timeoutMs": 30000,
  "maxTokens": 4096,
  "fast": true
}