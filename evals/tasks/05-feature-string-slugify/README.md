# Implement string slugify utility

**Category:** feature

### Prompt
In src/slugify.js, implement and export slugify(text). It should convert text to lowercase, replace non-alphanumeric characters with hyphens, collapse consecutive hyphens into a single hyphen, and trim hyphens from ends.
