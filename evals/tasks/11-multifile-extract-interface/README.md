# Extract shared role constants to constants.js

**Category:** multifile

### Prompt
Extract the ROLES object from src/auth.js into a new file src/constants.js. Export ROLES from src/constants.js and import it in both src/auth.js and src/permissions.js.
